---
const { image, title, description, aditionalclass } = Astro.props;
---

<div class={`flex flex-col items-center justify-center text-center py-12 shadow-md border px-6 ${aditionalclass}`}>
  <img src={image} alt={title} class="w-full h-12 mb-4 object-contain" loading="lazy" />
  <h4 class="text-xl font-semibold mb-2">{title}</h4>
  <p class="text-theme-rich-black/70">{description}</p>
</div>